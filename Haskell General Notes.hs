--Things of note

-- :t == check type
-- :r == reload file being used in program
-- to start haskell == ghci
-- to start with modules == ghci file.hs
--qsort :: Ord a => [a] -> [a]
-- how to read above
-- qsort is a function that applies to(::) class of ordered anys such that (=>) it takes in a list and outputs (->) a list 
